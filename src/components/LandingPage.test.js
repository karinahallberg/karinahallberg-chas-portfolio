import { render } from '@testing-library/react'
import LandingPage from './LandingPage'

describe('LandingPage Tests', () => {
  test('Check LandingPage renders', () => {
    render(<LandingPage />)
  })
})
